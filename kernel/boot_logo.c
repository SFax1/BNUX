#include <bnux/types.h>

extern void kprintf(const char *fmt, ...);

/* Lix — талисман BNUX (ArcXinch). Печатается в serial-консоль
 * при старте ядра, до остальных boot-сообщений. */
void print_lix_logo(void) {

}
